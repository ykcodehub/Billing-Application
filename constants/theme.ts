import Colors from "./colors";

const Theme = {
  colors: Colors,

  radius: {
    sm: 8,
    md: 12,
    lg: 20,
  },

  spacing: {
    xs: 5,
    sm: 10,
    md: 15,
    lg: 20,
    xl: 30,
  },
};

export default Theme;